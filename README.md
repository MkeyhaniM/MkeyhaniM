---

## 🎉 Hi , welcome


```javascript
// 🎩 Magician greet function
function greet(name) {
  const hats = ['🎩','🧢','👑','🎓'];
  const hat = hats[Math.floor(Math.random() * hats.length)];
  console.log(`${hat} Abracadabra, Hello there, ${name}! ${hat}`);
  console.log('✨✨ Let the magic begin! ✨✨');
}

greet('World'); // Try your own name and see the magic! 🪄
```
